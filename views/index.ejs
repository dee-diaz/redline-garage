<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head.ejs") %>
<body>

  <%- include("partials/header.ejs") %>

  <main>
    <%- include("partials/errors.ejs") %>
    <div class="page-container">
      <div class="listing-toolbar">
        <h1 class="listing-toolbar-title">featured products</h1>
      </div>

      <div class="listing-grid">
        <% if(featuredProducts.length > 0) { %>
          <% featuredProducts.forEach(function(product) { %>
            <%- include("partials/productCard.ejs", { product, wishlistIds }) %>
          <% }) %>
        <% } else { %>
          <p class="empty-state">couldn't load products right now</p>
        <% } %>
      </div>

      <div class="listing-footer">
        <a href="/products" class="btn-see-all">see all</a>
      </div>
    </div>
  </main>

  <%- include("partials/footer.ejs") %>

</body>
</html>
